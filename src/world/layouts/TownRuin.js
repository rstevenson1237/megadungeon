// src/world/layouts/TownRuin.js
// Grid of ruined building shells with open courtyards and collapsed walls.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement town ruin generation
  return [];
}
