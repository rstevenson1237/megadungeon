// src/world/layouts/CryptRows.js
// Parallel rows of burial alcoves separated by narrow aisles.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement crypt rows generation
  return [];
}
