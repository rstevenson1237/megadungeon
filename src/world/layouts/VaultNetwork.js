// src/world/layouts/VaultNetwork.js
// Rectangular vaults connected by short locked-door corridors.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement vault network generation
  return [];
}
