// src/world/layouts/BspRooms.js
// Classic binary-space-partitioned rectangular rooms connected by corridors.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: extract BSP carving from LevelGen._carveRooms
  return [];
}
