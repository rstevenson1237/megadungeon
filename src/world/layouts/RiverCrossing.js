// src/world/layouts/RiverCrossing.js
// A horizontal river bisects the level; rooms cluster on each bank.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement river crossing generation
  return [];
}
