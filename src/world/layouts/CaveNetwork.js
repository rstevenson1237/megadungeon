// src/world/layouts/CaveNetwork.js
// Organic cave passages carved with cellular-automaton flood-fill.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement cave network generation
  return [];
}
