// src/world/layouts/GreatHall.js
// One large central hall flanked by smaller side chambers.

/**
 * @param {TileMap} map
 * @param {RNG} rng
 * @param {Object} theme
 * @param {number} levelNumber
 * @returns {Array<Room>}
 */
export function generate(map, rng, theme, levelNumber) {
  // TODO: implement great hall generation
  return [];
}
